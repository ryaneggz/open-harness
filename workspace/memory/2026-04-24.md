# Memory — 2026-04-24

## slack-server-check heartbeat — 00:00 UTC
- **Result**: OP (FAIL reported)
- **Item**: heartbeat `slack-server-check` (schedule `*/5 * * * *`)
- **Action**: Ran the tmux probe in orchestrator context and via `docker exec` into `oh-local` sandbox. Both lack a tmux server and `/tmp/slack.log`. Slack bot not running.
- **Duration**: ~15s
- **Observation**: Heartbeat script assumes ambient tmux but orchestrator has none. Needs either (a) explicit `docker exec -u sandbox <sandbox>` wrapper, or (b) move heartbeat inside the target sandbox. Current candidates: `oh-local`, `portfolio-advisor`, `uat-tester`, `portfolio-mgr` — ambiguous which owns the Slack bot.

## slack-server-check heartbeat — rerun
- **Result**: OP (FAIL reported)
- **Item**: heartbeat `slack-server-check`
- **Action**: Re-probed orchestrator (no tmux), then `docker exec -u sandbox oh-local`. `SLACK_APP_TOKEN`/`SLACK_BOT_TOKEN` set, container up 36min, but no tmux server and no `/tmp/slack.log`. Slack bot did not auto-start.
- **Duration**: ~20s
- **Observation**: Auto-start condition (tokens present) met but no session spawned. Suggests entrypoint `tmux new-session -d -s slack ...` failed silently or is absent in current image. Needs orchestrator-initiated restart or bot boot inspection — heartbeat itself can only report, not remediate per scope.

## slack-server-check heartbeat — rerun (in-sandbox)
- **Result**: OP (FAIL reported)
- **Item**: heartbeat `slack-server-check`
- **Action**: Executed inside `oh-local` sandbox (hostname e2ce8f4dfd1e). `tmux ls` → no server. No `/tmp/slack.log`. Container up 41min, still no slack session.
- **Duration**: ~10s
- **Observation**: Third consecutive FAIL — slack bot has never been started in this sandbox's current lifecycle. Remediation out of scope for this heartbeat. Pattern confirmed: heartbeat reports, cannot self-heal.

## slack-server-check heartbeat — rerun (orchestrator)
- **Result**: OP (FAIL reported)
- **Item**: heartbeat `slack-server-check`
- **Action**: Probed orchestrator (no tmux server) and all four sandboxes (`oh-local`, `portfolio-advisor`, `uat-tester`, `portfolio-mgr`) via `docker exec -u sandbox <c> tmux ls` — all return `error connecting to /tmp/tmux-1000/default`. No slack session anywhere.
- **Duration**: ~15s
- **Observation**: Fourth consecutive FAIL. Slack bot not running in any active sandbox. Needs manual remediation (start tmux session w/ mom) or entrypoint fix — heartbeat continues to fire every 5min with no progress.

## slack-server-check heartbeat — rerun
- **Result**: OP (FAIL reported)
- **Item**: heartbeat `slack-server-check`
- **Action**: `tmux has-session -t slack` → fail. `tmux ls` shows only `t3code-app`. `/tmp/slack.log` absent.
- **Duration**: ~5s
- **Observation**: Fifth consecutive FAIL. State unchanged — no slack session exists. Heartbeat continues to surface signal but cannot remediate. User action required.
