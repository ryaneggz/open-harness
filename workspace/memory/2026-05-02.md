# 2026-05-02

## Nightly Release — 05:51 UTC
- **Result**: OP
- **Item**: nightly-release heartbeat — 73 commits since 2026.4.24 (last ancestor tag on development)
- **Action**: Cut release/2026.5.1 from development, promoted CHANGELOG, pushed branch+tag.
- **Duration**: ~3 min (CI poll dominant)
- **Observation**: `git describe --tags --abbrev=0` returns last *ancestor* tag, not latest tag — recent release tags (4.28/29/30) live on their own release branches and were never merged back, so describe walked past them to 4.24. Doesn't affect outcome (today's date drives version), but worth knowing.

## Release Workflow Findings
- The `?branch=<tag>&per_page=1` API returns the most recent *triggered* run, which can be the CI workflow (push event) — not the Release workflow. Got lucky polling but should filter by workflow name to be deterministic. Worth a `/release` skill PR.
- Release workflow (25245171759) → success. GitHub Release v2026.5.1 published.
- "Build Open Harness" workflow has been failing on every recent tag (4.29, 4.30, 5.1) with `Login to GitHub Container Registry: Password required`. Pre-existing — not a regression. Should open an issue.
- GHCR version-listing API needs `read:packages` scope; gh token lacks it — can't programmatically verify the image. Release workflow build/push step succeeded though.

## Resume Heartbeat — 10:31 UTC
- **Result**: SKIP (failure)
- **Item**: resume-bc471449-continue heartbeat — `claude --resume bc471449-449a-4e0f-b3f8-13541a10c24e -p "continue"`
- **Action**: Command exited 1 with `No conversation found with session ID: bc471449-449a-4e0f-b3f8-13541a10c24e`. Session is stale.
- **Duration**: ~1s
- **Observation**: Resume-by-id heartbeats become stale when the underlying session is GC'd. Should either (a) drop the heartbeat once its task is done, or (b) point at a still-live session. Current schedule will keep failing nightly until removed/updated.
