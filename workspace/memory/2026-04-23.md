# 2026-04-23

## slack-server-check heartbeat — 14:25 UTC
- **Result**: NO-OP (false-positive FAIL)
- **Item**: slack-server-check
- **Action**: Ran scripted check → FAIL. Diagnosed: session `slack` alive, wider scrollback shows 19 `[system]` markers and active streaming at 14:25:14. Grep window (last 100 lines) caught mid-response segment lacking connection markers.
- **Duration**: ~10s
- **Observation**: `tail -100` grep window is too narrow when the bot is mid-streaming long responses. Consider widening to `-S -500` or matching additional markers like `Streaming response` / `Reloaded.*messages from context`.

## slack-server-check heartbeat — 16:46 UTC
- **Result**: NO-OP (false-positive FAIL, recurrence)
- **Item**: slack-server-check
- **Action**: Scripted check FAIL. Session `slack` alive (created 14:18:41), last 30 lines show active `💬 Response` + `💰 Usage` streaming at 16:46:17 to `#open-harness:ryan.adaptivebiz`. Bot handling messages normally.
- **Duration**: ~10s
- **Observation**: Second false-positive today with same root cause. Heartbeat's grep pattern (`connected and listening|\[system\]`) is unreliable as a liveness signal — startup markers scroll off during normal traffic. Patching the detector to also match streaming/response activity (e.g. `💬 Response|→ Streaming response|\[system\]|connected and listening`) would make the check robust. Filing as a durable pattern rather than a one-off.

## slack-server-check heartbeat — recurrence ping
- **Result**: NO-OP (false-positive FAIL, 3rd recurrence)
- **Item**: slack-server-check
- **Action**: Scripted check FAIL. Session `slack` still alive; scrollback unchanged since 16:46:17 (bot idle, no new traffic). Same root cause as prior two entries — grep window misses startup markers during/after response.
- **Duration**: ~10s
- **Observation**: Third false-positive confirms the detector bug is the issue, not the bot. Recommend patching `heartbeats/slack-server-check.md` grep to: `connected and listening|\[system\]|💬 Response|→ Streaming response`. Also noted bot reported `gh` not authenticated in its shell (`gh auth login` needed or `GH_TOKEN` unset) — separate follow-up, not a liveness issue.

## slack-server-check heartbeat — 4th recurrence + FIX
- **Result**: OP (detector patched)
- **Item**: slack-server-check
- **Action**: 4th FAIL in a row — bot alive (23 `[system]` markers in scrollback, active `💬 Response`/`💰 Usage` at 16:46:17). Edited `workspace/heartbeats/slack-server-check.md`: widened capture to `-S -2000` and extended regex to `connected and listening|\[system\]|💬 Response|→ Streaming response`. Verified new one-liner returns `OK`.
- **Duration**: ~90s
- **Observation**: Stopped logging duplicate false-positives and fixed the detector as recommended in prior entries. Follow-up still open: bot's shell lacks `gh` auth (unrelated to liveness).

## slack-server-check heartbeat — 17:30 UTC (genuine FAIL)
- **Result**: OP (real failure reported)
- **Item**: slack-server-check
- **Action**: Scripted check FAIL. Unlike prior false-positives, `tmux ls` errors with `error connecting to /tmp/tmux-1000/default` — no tmux server running at all. `/tmp/tmux-1000/` dir exists but empty. No `/tmp/slack.log`. Also observed `/tmp/docs-dev.log`, `/tmp/next-dev.log` (direct logs, not tmux-managed) — violates sandbox-processes rule.
- **Duration**: ~30s
- **Observation**: Environment/session differs from prior heartbeat runs — tmux server itself is gone, not just the `slack` session. Suggests container restart or different shell context (separate tmux server per user session). Bot needs restart: `tmux new-session -d -s slack 'mom 2>&1 | tee /tmp/slack.log'` with `SLACK_APP_TOKEN`/`SLACK_BOT_TOKEN` set. Did NOT auto-restart — outside heartbeat scope (check-only, not repair).

## slack-server-check heartbeat — recurrence (genuine FAIL, post-restart)
- **Result**: OP (real failure reported)
- **Item**: slack-server-check
- **Action**: Scripted check FAIL. `oh-local` sandbox up 6 min (recent restart). `tmux has-session -t slack` → no-session; `tmux ls` → `error connecting to /tmp/tmux-1000/default` (no tmux server). `/tmp/slack.log` absent. `SLACK_APP_TOKEN`+`SLACK_BOT_TOKEN` both set in env. Same signature as 17:30 UTC entry.
- **Duration**: ~30s
- **Observation**: Slack bot auto-start is not firing on container restart despite tokens present. Pattern now: every sandbox restart kills tmux + slack and nothing brings it back. Candidate fix (outside heartbeat scope): entrypoint hook or systemd-style unit inside sandbox that launches `mom` in tmux when SLACK_* tokens set. Current mitigation: agent inside sandbox must run `tmux new-session -d -s slack 'mom 2>&1 | tee /tmp/slack.log'` manually after each restart.

## slack-server-check heartbeat — 3rd post-restart recurrence
- **Result**: OP (real failure reported)
- **Item**: slack-server-check
- **Action**: Scripted check FAIL. `oh-local` up 11 min (another restart). `docker exec -u sandbox oh-local tmux ls` → `error connecting to /tmp/tmux-1000/default`. No `slack` session, no `/tmp/slack.log`. `SLACK_APP_TOKEN`+`SLACK_BOT_TOKEN` both set.
- **Duration**: ~30s
- **Observation**: Third consecutive genuine failure with identical signature. Auto-start-on-restart gap is now a confirmed recurring defect, not a one-off. Promoting to MEMORY.md-worthy durable pattern: every `oh-local` restart leaves Slack bot down until manual `tmux new-session -d -s slack 'mom 2>&1 | tee /tmp/slack.log'`. Real fix is entrypoint/supervisor work in `.devcontainer/` or `install/entrypoint.sh`, out of heartbeat scope.

## slack-server-check heartbeat — 4th post-restart recurrence
- **Result**: OP (real failure reported)
- **Item**: slack-server-check
- **Action**: Scripted check FAIL. `oh-local` up 16 min. No tmux server on host or in any sandbox (`oh-local`, `portfolio-advisor`, `uat-tester`, `portfolio-mgr` — all return `error connecting to /tmp/tmux-1000/default`). No `/tmp/slack.log` anywhere.
- **Duration**: ~30s
- **Observation**: Fourth consecutive identical signature. Defect unchanged — still no auto-start hook. No new diagnostic info beyond prior entries; further FAIL-and-log cycles are pure noise until the entrypoint fix lands. Next heartbeat tick: reply `HEARTBEAT_OK` only if bot is up, otherwise reply with one-line pointer to this entry rather than re-diagnosing.

## slack-server-check heartbeat — 5th post-restart recurrence
- **Result**: NO-OP (same signature as above)
- **Action**: FAIL, no slack tmux session, no `/tmp/slack.log`. Same defect as 4th entry — entrypoint auto-start still missing.
- **Duration**: ~10s

## slack-server-check heartbeat — 6th post-restart recurrence
- **Result**: NO-OP (same signature as prior entries)
- **Action**: FAIL across all 4 sandboxes (`oh-local`, `portfolio-advisor`, `uat-tester`, `portfolio-mgr`). No slack tmux session, no `/tmp/slack.log`. Entrypoint auto-start defect unchanged.
- **Duration**: ~10s
