{
    "schemaVersion": 1,
    "project": "Open Harness",
    "branchName": "task/artifact-contract-fixture",
    "description": "FIXTURE — NOT a real task. Exercises /audit Gate 1 sub-check (b), the artifact-contract enforcement. Its root artifact_contract.required_artifacts declares one path that exists (this fixture itself) and one that deliberately does NOT exist (MISSING-ON-PURPOSE.md), so Gate 1 must emit AUDIT-FAIL. Do NOT create the missing path — its absence is the point. See docs/artifact-contract-schema.md and .agro/evals/probes/artifact-contract-audit.sh.",
    "artifact_contract": {
        "required_artifacts": [
            ".agro/skills/audit/fixtures/artifact-contract.prd.json",
            ".agro/skills/audit/fixtures/MISSING-ON-PURPOSE.md"
        ],
        "allowed_locations": [
            ".agro/skills/audit/fixtures/"
        ],
        "verification_commands": [
            "bash .agro/evals/probes/artifact-contract-audit.sh"
        ]
    },
    "userStories": [
        {
            "id": "US-001",
            "title": "Fixture placeholder story",
            "description": "Structural placeholder so this fixture is a valid prd.json; the fixture's purpose is its artifact_contract block, not its task graph.",
            "acceptanceCriteria": [
                "Gate 1 artifact-contract sub-check FAILs because MISSING-ON-PURPOSE.md is absent"
            ],
            "priority": 1,
            "passes": true,
            "notes": "Fixture only — see the root artifact_contract block."
        }
    ]
}
