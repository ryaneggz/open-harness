# Oracle: DS-001 — Add default Pi Monitor support

The trajectory adds default Pi Monitor support by adopting the upstream Pi package `@trevonistrevon/pi-loop`. It pins `npm:@trevonistrevon/pi-loop@0.5.5` in `.pi/settings.json` and asserts that exact pin in the Pi settings regression test (`.pi/extensions/__tests__/settings.test.ts`). It documents `/loop`, the `Loop*` tools, and the `Monitor*` tools in `docs/harnesses/pi.md`, presenting Monitor as the primary reason for pulling in the package. Runtime loop state under `.pi/loops/` is treated as local, gitignored session/project state (via `.pi/.gitignore`), with `PI_LOOP_SCOPE=session` kept as the default. Provenance is captured as an immutable wiki snapshot (`wiki/raw/2026-06-14-pi-loop.md`) plus a synthesized entity page (`wiki/pi-loop.md`), and `wiki/README.md` is regenerated to index the new entry. The change is recorded in `CHANGELOG.md` and clears the eval gate, with `evals/RESULTS.md` refreshed.

Reward: scored by `diff_similarity` — changed-file overlap of a candidate diff against `oracle/changed-files.txt` (the 9 files in merged PR #147) — plus `artifact_presence`, satisfied when PR #147 is merged and the listed files exist in the repo.
