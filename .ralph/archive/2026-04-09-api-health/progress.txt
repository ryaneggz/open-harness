# Ralph Progress Log
Started: Wed Apr  8 20:54:00 MDT 2026
---

## Codebase Patterns
- Next.js app lives in `workspace/next-app/` inside the monorepo root (not at repo root `src/`)
- Prisma 7 requires `@prisma/adapter-pg` — cannot instantiate `PrismaClient()` without an adapter
- Generated Prisma client is in `src/generated/prisma/` but gitignored — run `npx prisma generate` before type-checking
- Import PrismaClient from `@/generated/prisma/client` (not `@/generated/prisma`)
- Quality checks: `npm run type-check`, `npm run lint`, `npm test` from `workspace/next-app/`
- Pre-commit hooks run lint-staged + build + tests for `cli/` and `packages/sandbox/` workspaces too
- Run `npx tsc --noEmit` from `workspace/next-app/` (nested), NOT from git root — path aliases and types only resolve from nested tsconfig
- Root `package.json` is monorepo config — install Next.js app deps in `workspace/next-app/package.json`
- Run `npx prisma generate` from `workspace/next-app/` where prisma.config.ts lives

## 2026-04-08 21:01 UTC - US-001
- Created `src/lib/prisma.ts` — PrismaClient singleton using `@prisma/adapter-pg`
- Created `src/app/api/health/route.ts` — GET handler returning `{ status, timestamp, uptime, services: { nextjs } }`
- Installed `@prisma/adapter-pg` dependency
- Files changed: `workspace/next-app/src/lib/prisma.ts`, `workspace/next-app/src/app/api/health/route.ts`, `workspace/next-app/package.json`, `workspace/next-app/package-lock.json`
- **Learnings for future iterations:**
  - Repo is a monorepo — root `package.json` has workspaces `["cli", "packages/sandbox"]`, Next.js app is at `workspace/next-app/`
  - Prisma 7 changed: `new PrismaClient()` no longer works — requires `{ adapter: new PrismaPg({ connectionString }) }`
  - `src/generated/prisma/` is gitignored, must regenerate with `npx prisma generate` in a fresh checkout
  - Pre-commit hooks validate ALL workspaces, not just workspace/next-app
---

## 2026-04-09 03:06 UTC - US-002
- Added PostgreSQL health check via `prisma.$queryRawUnsafe("SELECT 1")` with Promise.race timeout guard (4s)
- On success: `services.database = "ok"`, status `"ok"`, HTTP 200
- On failure: `services.database = "error"`, status `"degraded"`, HTTP 503
- Files changed: `workspace/next-app/src/app/api/health/route.ts`
- **Learnings for future iterations:**
  - Run `npx tsc --noEmit` from `workspace/next-app/` (nested dir), NOT from the git root — the `@/` alias and `next` types only resolve correctly from the nested tsconfig
  - The git root is `/home/sandbox/workspace/next-app/` but the Next.js source lives at `workspace/next-app/src/` within it — all source file paths are relative to git root
  - Root `package.json` is monorepo config only — do NOT install Next.js app deps there; they belong in `workspace/next-app/package.json`
  - `npx prisma generate` must be run from `workspace/next-app/` where prisma.config.ts lives
- `workspace/` is gitignored — use `git add -f` to stage files under that path
- Mock Prisma with `vi.mock("@/lib/prisma", ...)` at module level; vitest hoists it
- Import Next.js route handlers directly for unit tests — no HTTP server needed
---

## 2026-04-09 03:09 UTC - US-003
- Created `src/app/api/health/__tests__/route.test.ts` with 4 unit tests
- Test: GET returns 200 with status 'ok' when database is reachable
- Test: Response includes valid ISO-8601 timestamp, numeric uptime >= 0, and services object
- Test: GET returns 503 with status 'degraded' when database query throws
- Test: services.database is 'error' when database is unreachable
- Mocked `@/lib/prisma` using `vi.mock` to control `$queryRawUnsafe` behavior per test
- Files changed: `workspace/next-app/src/app/api/health/__tests__/route.test.ts`
- **Learnings for future iterations:**
  - Use `vi.mock("@/lib/prisma", ...)` at module level to mock the Prisma singleton — vitest hoists it automatically
  - Import the route handler directly (`import { GET } from "../route"`) — no HTTP server needed
  - `workspace/` is gitignored — must use `git add -f` to stage files under that path
  - Pre-commit hooks run cli/ and packages/sandbox/ workspaces too (build + test for each)
---
