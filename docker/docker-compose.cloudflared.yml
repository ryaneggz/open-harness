services:
  sandbox:
    environment:
      - INSTALL_CLOUDFLARED=true
      - INSTALL_BROWSER=true
    volumes:
      - cloudflared:/home/sandbox/.cloudflared
      - gh-config:/home/sandbox/.config/gh
      - ssh-keys:/home/sandbox/.ssh

volumes:
  cloudflared:
  gh-config:
  ssh-keys:
