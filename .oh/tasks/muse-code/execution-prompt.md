Implement https://github.com/mifunedev/openharness/issues/952 with the operator's clarification: the ticket is outdated. Add first-class support to `oh harness install muse-code` through the current install-only lifecycle. `oh init` is retired. This clarification supersedes all retired lifecycle requirements below: do not restore install.museCode, persisted intent, boot provisioning, optional catalog kinds, or oh init. Preserve binaries and auth through the existing home volume; fresh homes require the install verb. Create the standard skills surface through the canonical linker used by sandbox bootstrap.

Read cumulative AGENTS.md, applicable canonical skills, and directory READMEs. Preserve unrelated work and use an isolated feature worktree. Follow the spec workflow; the operator has authorized generating and executing this prompt within the reconciled issue scope. Keep one implementation owner. First delegate the issue's three independent read-only explorations: official Muse upstream behavior, harness lifecycle, and provider/context surfaces. Reconcile all findings yourself.

Verify official current installer source and docs: exact command, prefix and profile controls, platform requirements, binary/version, auth/config locations, login, META_API_KEY, AGENTS.md semantics, and Agent Skills discovery. Do not invent upstream flags or substitute mocked tests for real upstream evidence.

Before code changes, score the five historical approaches and the current installable-catalog approach against all ten criteria. Record the architecture decision and rejected alternatives. Write a plan mapping every acceptance item to files and validation. Mark obsolete requirements as superseded by the operator. Mark every required repository surface applied or not applicable.

Add muse-code as kind installable through the existing catalog and persistent sandbox home. Install as sandbox, verify muse --version, preserve idempotence, stopped-sandbox refusal and unchanged oh.json. Control profile mutations and suppress interactive download authentication. Create/repair .agents/skills -> ../.oh/skills through the canonical linker used by sandbox bootstrap. Track the link for clean clones. Preserve canonical AGENTS.md and .oh/skills ownership. Respect the reconciled non-goals.

Add meaningful coverage for catalog metadata, live/already-installed/stopped/failing installation, unchanged configuration, and provider link creation/repair/collision preservation. Preserve the guard against retired project-agent catalogs while allowing the standard skills path. Support META_API_KEY through the existing secret allowlist and inert example schema; test hidden input, storage and redaction. Document verified authentication and explicit process injection without implying secret storage exports variables. Document context/trust, skills, persistence/recreation, explicit reinstall after home loss, removal, and upstream limitations. Update relevant overview/configuration/installation/quickstart/layout docs and changelog. Mirror scoped public docs in the website's separate repository and worktree.

Run relevant tests, the full repository suite, typecheck/build, and required eval/CI gates. Repair regressions and record commands and observed outputs. Exercise the real installer through the built oh CLI in a disposable home without changing operator credentials or profiles. Prove ownership/prefix, profile behavior, repeat-install safety, version/status and skill discovery. Use a credential-free diagnostic provider if available. Do not spend model credits or restart/destroy the active workspace. Distinguish real recreation, process persistence, mocks, official sources, and unavailable authenticated verification. Reconcile affected knowledge from the actual diff.

Review the final diff against every acceptance checkbox. Commit evidence covering benefit, requested and actual behavior, divergence, and unverified items. Open/update the issue-linked PR against the correct remote/base and verify CI for its exact head. Mark Ready only when every applicable acceptance item and required gate is supported; otherwise leave it draft with concrete blockers. Do not merge. Return this prompt's location, PR URL/status, validation results, and remaining gaps. Continue authorized work without redundant confirmation.
